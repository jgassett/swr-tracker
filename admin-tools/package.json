{
  "name": "swr-tracker-admin-tools",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local admin utilities for the SWR Tracker Firebase project (not deployed).",
  "scripts": {
    "reset-pin": "node reset-pin.mjs",
    "purge-qb-sandbox": "node purge-qb-sandbox.mjs"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
